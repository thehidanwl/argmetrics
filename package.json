{
  "name": "argmetrics",
  "version": "1.0.0",
  "private": true,
  "description": "ArgMetrics - Argentine Economic Data Platform",
  "scripts": {
    "dev": "echo 'Use cd backend && npm run dev'",
    "build": "cd backend && npm install && npx prisma generate && npm run build",
    "build:web": "cd web && npm install && npm run build",
    "build:api": "cd backend && npm install && npx prisma generate && npm run build"
  },
  "dependencies": {
    "@prisma/client": "^5.9.0"
  },
  "devDependencies": {
    "@vercel/node": "^5.6.12",
    "prisma": "^5.9.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
